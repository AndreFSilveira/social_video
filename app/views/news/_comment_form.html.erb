<%= simple_form_for ([@news, @news.comments.build ]) do |f| %>

  <%= f.input :user_id, :as => :hidden, :input_html => { :value => current_user.id} %>
  <%= f.input :content %>
  <%= f.button :button %>
<% end %>