<% provide(:title, t("page.title.videos.create") ) %>
<div class="video-create container">
  <h1 class="center"><%= t("page.title.videos.create") %></h1>
  <div class="row">
    <div class="col s12 m8 offset-m2">
      <%= render 'form' %>
    </div>
  </div>

  <%= link_to videos_path, class: 'waves-effect waves-light btn lime darken-2 bottom-link' do %>
      <i class="material-icons left">reply</i>
      <%= t("page.news.action.new.back")%>
  <%end%>

</div>